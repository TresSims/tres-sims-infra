---
apiVersion: v1
kind: Namespace
metadata:
  name: tailscale
  labels:
    # Proxy pods run privileged (they configure sysctls and create a tun
    # device), which the cluster-wide "baseline" default rejects.
    pod-security.kubernetes.io/enforce: privileged
